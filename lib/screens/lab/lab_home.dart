import 'package:flutter/material.dart';

class LabHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Lab Home")),
      body: Center(child: Text("Lab Dashboard")),
    );
  }
}